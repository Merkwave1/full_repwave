// src/components/common/VersionSync.jsx
// Version sync removed; simple passthrough component retained for compatibility.
const VersionSync = ({ children }) => children || null;
export default VersionSync;
